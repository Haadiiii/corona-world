import React from "react";
import { NavLink } from "react-router-dom";

const Corona = () => (
  <div>
    <NavLink to="/Details">
      <div>haadiii</div>
    </NavLink>
  </div>
);

export default Corona;
